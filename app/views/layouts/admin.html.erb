<!DOCTYPE html>
<html>
  <head>
    <title>餐廳訂位系統 - 管理後台</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true, type: "module" %>
  </head>

  <body class="bg-gray-50">
    <!-- 導覽列 -->
    <nav class="bg-white shadow-sm border-b">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to admin_root_path, class: "text-xl font-bold text-gray-900" do %>
                餐廳管理系統
              <% end %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to admin_root_path, 
                  class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" do %>
                儀表板
              <% end %>
              <%= link_to admin_users_path, 
                  class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" do %>
                管理員管理
              <% end %>
              <%= link_to admin_restaurants_path, 
                  class: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700 whitespace-nowrap py-2 px-1 border-b-2 font-medium text-sm" do %>
                餐廳管理
              <% end %>
            </div>
          </div>
          <div class="flex items-center">
            <div class="ml-3 relative">
              <div class="flex items-center space-x-4">
                <%= link_to root_path,
                    class: "text-gray-500 hover:text-gray-700 text-sm" do %>
                  回到首頁
                <% end %>
              </div>
            </div>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash 訊息區域 -->
    <div id="flash_messages" class="fixed top-4 right-4 z-50">
      <%= render 'shared/flash' if notice || alert %>
    </div>

    <!-- 主要內容 -->
    <main>
      <%= yield %>
    </main>
    
    <!-- 全域確認對話框 -->
    <%= render 'shared/confirmation_modal' %>
  </body>
</html> 