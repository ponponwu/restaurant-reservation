<%= turbo_stream.replace "reservation_#{@reservation.id}" do %>
  <%= render "reservation_row", reservation: @reservation %>
<% end %>

<%= turbo_stream.update "flash_messages" do %>
  <%= render "shared/flash", notice: "訂位已完成" %>
<% end %> 